import "../global.css";

export const metadata = {
  title: 'EDUTION - Notifications',
  description: 'Notification page of EDUTION. Nepals one and only social media site with the feature of learning integrated.',
}

export default function Layout({ children }) {
  return (
    <>
      {children}
    </>
  )
}